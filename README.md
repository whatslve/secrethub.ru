1 - устанавливаешь докер

2 - копируем в корне .env.example и переименовываем в .env задаем свои параметры

3 - копируем в backend .env.example и переименовываем в .env задаем свои параметры

4 - устанавливаем sudo apt update && sudo apt install nodejs npm
